#include <iostream>
#include <fstream>
#include <cmath>
#include <algorithm>
#include <string>
#include <vector>
#include <iomanip>

using namespace std;

int main()
{
    unsigned long long n, c;
    string s;
    cin >> n;
    
    for (int i = 0; i < n; i++)
    {
        cin >> s;
        int m = 0;
        /*if (c == 0)
            m++;
        while (c > 0)
        {
            m++;
            c /= 10;
        }*/
        
        cout << s.length() << endl;
        
    }
    
    return 0;
}
